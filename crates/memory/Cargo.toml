[package]
name = "mc-memory"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Memory system (MEMORY.md / USER.md) for mavis-claw"

[dependencies]
mc-core = { workspace = true }

[dev-dependencies]
tempfile = "3"
