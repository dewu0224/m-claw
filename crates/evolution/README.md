# mc-evolution

Evolution system (Background Review + Curator) for mavis-claw.

**Status:** Stub — not yet implemented.

## Planned Contents

- `BackgroundReviewer` (nudge counters, spawn review tasks)
- `NudgeConfig` (memory_interval, skill_interval)
- `Curator` (automatic state transitions, LLM consolidation pass)
- `SkillUsage` (use_count, view_count, patch_count, state)
- `SkillState` enum (Active, Stale, Archived, Pinned)
